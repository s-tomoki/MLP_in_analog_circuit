"""活性化関数を集めたモジュール。

ステージ1ではステップ関数だけを扱う。
"""


def step(x):
    """ステップ関数。

    ニューロンが「発火するかどうか」を 0 / 1 で表す。

    Args:
        x: スカラー、または NumPy 配列。

    Returns:
        x と同じ形の値。各要素は 0 または 1。
        入力がちょうど 0 のときは 0 とする。
    """
    # ヒント:
    # - 0 より大きい要素は 1、それ以外(0 ちょうどを含む)は 0 にする
    # - スカラーが来ても配列が来ても動くようにする
    # - 配列の場合、for 文で 1 要素ずつ処理しなくても NumPy の比較演算は要素ごとに働く
    raise NotImplementedError
